# Copy this file to ".env" for local dev, or set these as variables in Railway.
#
# AI generation is OPTIONAL. The app works as a full template editor with NO key.
# Set ANTHROPIC_API_KEY to enable the "Generate with AI" button.
# Get a key at https://console.anthropic.com/

# ANTHROPIC_API_KEY=sk-ant-...

# Optional: model used for AI generation. Defaults to claude-sonnet-4-6.
# CLAUDE_MODEL=claude-sonnet-4-6

# AI gate (STRONGLY recommended on a public deploy with a key set).
# If AI_PASSWORD is set, only the paid "Generate with AI" route requires this
# password — the editor and Word/HTML exports stay open for everyone. The browser
# prompts for it once and remembers it. Leave unset to keep AI generation open.
# AI_PASSWORD=choose-a-long-random-passphrase

# Optional: port (Railway sets this automatically).
# PORT=3000
